import { EventsContainer, SearchContainer } from '../../components';

function AllEvents() {
    return (
        <>
            <SearchContainer />
            <EventsContainer />
        </>
    );
}

export default AllEvents;
